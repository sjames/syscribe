---
type: Package
name: HTML
---

Integration test cases for the `export-html` subcommand. Each is realised as a black-box Rust
integration test that runs `syscribe export-html` against a fixture model and inspects the
generated static site.
