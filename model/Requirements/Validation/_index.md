---
type: Package
name: Validation
---

Requirements governing general validator behavior — checks that apply uniformly across element
origins (hand-authored Markdown, SysMLv2-synthesized, or any future ingestion path) rather than
being scoped to one specific feature area. Distinct from `Schema` (frontmatter field
recognition/strictness); this package covers semantic checks like documentation completeness.
