---
type: Package
name: Schema
---

Requirements governing frontmatter **schema strictness**: which fields the parser
recognizes, how unrecognized fields are reported, and the sanctioned channel
(`custom_fields:`) for author-defined metadata. Motivated by silent field-dropping,
where a mistyped or ad-hoc key is accepted and discarded with no feedback.
