---
type: Package
name: InterfaceImplTrace
---

Requirements for extending the implementation trace (`implementedBy:` / W023) to
`Interface` and `InterfaceDef` elements.

All requirements derive from `REQ-TRS-IFACE-000` and are governed by
`ADR-SYS-IFACE-001`.
