---
type: Package
name: Ratelimit
---

Root package for a small Python library that rate-limits API calls.

Layout:

- `Requirements` — what the library must do (`REQ-RL-NNN`)
- `Design` — decisions and design elements (`ADR-RL-NNN`)
- `Tests` — test cases and plans (`TC-RL-NNN`, `TP-RL-NNN`)
- `Plan` — work items (`PI-RL-NNN`)

Id prefix for this project: `RL`.
