---
type: FeatureDef
id: FEAT-VEHICLE-ROOT
name: Vehicle
groupKind: mandatory
---

Root feature of the vehicle product line. The vehicle tier owns no parameters of its own in this
worked example — its entire `parameterBindings:` role is closing what the consolidated subtree
still leaves open (see the two `Configuration`s below).
