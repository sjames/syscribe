---
type: FeatureDef
id: FEAT-LEGACY-SAFEMODE
name: SafeMode
groupKind: optional
---

A per-file `FeatureDef`, authored the *old* way, referenced from the
`FeatureModel` sheet's `crossTreeConstraints:` by its stable id — proof the
two forms interoperate freely in one model.
