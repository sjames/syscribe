---
type: Package
name: PlanningItemErrorDemo
---

Deliberate, isolated demonstration of `REQ-TRS-PLANITEM-006`'s leaf-evidence
rule (`E719`) actually firing — kept as its own standalone model root so the
main `../../model/` example stays a clean, 0-error demonstration. See
`../../README.md`'s "Deliberate error demonstration" section for the expected
output and why each item fails.
