# Root marker + MagicGrid gate profile for TC-TRS-MG-001.
# The magicgrid profile enables the gated MG### checks and promotes W307
# (non-draft UseCaseDef with no refines) into a gate failure (non-zero exit).
[profiles.magicgrid]
magicgrid = true
promote = ["W307"]
