---
type: Package
name: Features
featureTree:
  - name: Wdt
    id: FEAT-FM5-WRONGTYPE
    groupKind: optional
---

`featureTree:` declared on a plain `Package`, not a `FeatureModel` — inert, flagged.
