---
type: FeatureModel
name: Features
featureTree:
  - name: X
    groupKind: optional
---

A single-character name derives 'FEAT-X', which fails the FEAT-* grammar
(min 2 chars per segment) — must surface as the existing E006, not a new code.
