---
id: TC-002
type: TestCase
name: Valid bare TC id (no category segment)
status: draft
testLevel: L3
verifies:
  - REQ-TST-XRF-001
---

`TC-002` — no category segment between the prefix and the numeric suffix. Valid since
`REQ-TRS-ID-002`/`ADR-SYS-ID-*` widened the `TC-*` pattern to make the category segment(s)
optional (`*`, not `+`).

```gherkin
Feature: Test fixture

  Scenario: Baseline
    Given the system is configured
    When the action is performed
    Then the expected result occurs
```
